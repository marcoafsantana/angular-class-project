import { NgElseDirectiveDirective } from './ng-else-directive.directive';

describe('NgElseDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new NgElseDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
