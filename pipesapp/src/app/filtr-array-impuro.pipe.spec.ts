import { FiltrArrayImpuroPipe } from './filtr-array-impuro.pipe';

describe('FiltrArrayImpuroPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrArrayImpuroPipe();
    expect(pipe).toBeTruthy();
  });
});
